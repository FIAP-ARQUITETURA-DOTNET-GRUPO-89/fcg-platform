services:

  ####################################################
  # POSTGRES
  ####################################################

  postgres:
    image: postgres:17
    container_name: fcg-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - fcg-network

  ####################################################
  # RABBITMQ
  ####################################################

  rabbitmq:
    image: rabbitmq:3-management
    container_name: fcg-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - fcg-network

  ####################################################
  # USERS API
  ####################################################

  users-api:
    image: jondamiao/fcg-users-api:1.0.0-preview.3
    container_name: fcg-users-api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:7010
      RUN_MIGRATION: "true"

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgusers-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    ports:
      - "7010:7010"

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

    networks:
      - fcg-network

  ####################################################
  # CATALOG API
  ####################################################

  catalog-api:
    image: jondamiao/fcg-catalog-api:1.0.0-preview.3
    container_name: fcg-catalog-api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:7030
      RUN_MIGRATION: "true"

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgcatalog-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    ports:
      - "7030:7030"

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

    networks:
      - fcg-network

  ####################################################
  # CATALOG WORKER
  ####################################################

  catalog-worker:
    image: jondamiao/fcg-catalog-worker:1.0.0-preview.3
    container_name: fcg-catalog-worker
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgcatalog-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      catalog-api:
        condition: service_started

    networks:
      - fcg-network

  ####################################################
  # PAYMENTS API
  ####################################################

  payments-api:
    image: jondamiao/fcg-payments-api:1.0.0-preview.3
    container_name: fcg-payments-api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:7040
      RUN_MIGRATION: "true"

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgpayments-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    ports:
      - "7040:7040"

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

    networks:
      - fcg-network

  ####################################################
  # PAYMENTS WORKER
  ####################################################

  payments-worker:
    image: jondamiao/fcg-payments-worker:1.0.0-preview.3
    container_name: fcg-payments-worker
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgpayments-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      payments-api:
        condition: service_started

    networks:
      - fcg-network

  ####################################################
  # NOTIFICATIONS WORKER
  ####################################################

  notifications-worker:
    image: jondamiao/fcg-notifications-worker:1.0.0-preview.6
    container_name: fcg-notifications-worker
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      RUN_MIGRATION: "true"

      JwtSettings__Issuer: FcgUsers-Issuer
      JwtSettings__SecurityKey: FcgUsers_Secret_Key_2026_High_Security_Token
      JwtSettings__ExpirationHours: "2"

      ConnectionStrings__Default: Host=postgres;Port=5432;Database=fcgnotifications-db;Username=postgres;Password=postgres
      ConnectionStrings__Rabbitmq: amqp://guest:guest@rabbitmq:5672

    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

    networks:
      - fcg-network

volumes:
  postgres-data:

networks:
  fcg-network:
    driver: bridge